---
status: passed
subject: nbdev notebook layer removal (card zmriapqL, PR #3)
created: 2026-07-05
mode: pipeline
---

# Evidence: nbdev notebook layer removal

## Verdict
Fresh test, lint, import-smoke, and wheel-build runs all confirm the notebook layer is fully removed with no behavioral or packaging regressions.

## What Changed
- Deleted `nbs/`, `merceka_core/_modidx.py`, `settings.ini`
- Stripped `AUTOGENERATED` headers and `# %%` cell markers from `llm.py` / `evaluation.py`
- Dropped the `notebooks` extra and nbdev/jupyter dev deps; metadata test pins their absence
- Archived stale `evaluation_design.md` / `evaluation_plan.md` to `docs/archive/`
- Rider: fixed 19 pre-existing ruff violations, including two never-defined names in `image.py` `__all__`

## Evidence Captured
| Type | Artifact / Command | Result |
|------|--------------------|--------|
| test | `uv run pytest tests/` | 185 passed, 1 skipped, 6 deselected |
| lint | `uv run ruff check .` | clean |
| fs | `ls nbs merceka_core/_modidx.py settings.ini` | all absent |
| import smoke | `import merceka_core.{llm,evaluation,image}`; `from merceka_core.image import *`; `litellm.suppress_debug_info` | ok — star-import that previously raised AttributeError now works; litellm side-effect intact |
| packaging | `uv build` + wheel content grep | wheel builds; zero nbdev artifacts inside |

## Reviewer Assessments
| Reviewer | Status | Result |
|----------|--------|--------|
| ce-correctness-reviewer | passed | AST-diff vs main: only dead-import removals, no function-body changes |
| ce-api-contract-reviewer | passed | 0 downstream breaks across 6 consumer repos; 17/17 contract tests pass |

## Analysis
Not required — passed.

## Gaps
- Out-of-repo scripts installing `merceka-core[notebooks]` would fail with unknown-extra (no known consumers; grep of all six sibling repos found none).

## Next Action
None
